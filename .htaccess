# Turn on the rewrite engine
RewriteEngine On

# Send all requests that aren't real files/folders to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.php [L]